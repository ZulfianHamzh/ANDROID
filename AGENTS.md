# ANDROID — Project Journal

Persistence anchor for this workspace's agent memory. The agent maintains this file:
append notable decisions, changes, and session notes so they survive across chats and
sessions. Newest entries on top. `get_project_briefing` reads the sections below.

## About

Workspace untuk **DHBH Pos** (`dhbh_app`) — aplikasi Point of Sale Flutter untuk klinik kesehatan multi-cabang.
Online-only (Supabase `jiunlvlcwsntjbyybszd` sebagai single source of truth; SQLite hanya untuk backup manual).
Windows desktop adalah target utama (printing via `printing` package); Android/iOS juga didukung (Bluetooth thermal printer).

## Recent Changes

- **2026-08-15 — Diskon (persen/nominal) + filter cabang + Hold Order multi-pelanggan**:
  1) **DB (via Management API)**: `transactions` + `discount integer NOT NULL DEFAULT 0` (total_amount = GRAND TOTAL setelah diskon; getter `subtotal = totalAmount + discount`); `held_orders` + `customers jsonb NOT NULL DEFAULT '[]'` (array nama) + backfill dari customer_name. CATATAN: tabel `transactions` & `held_orders` saat itu KOSONG (0 baris) — data sudah kosong sebelum sesi (bukan akibat migrasi; branches=2 = koneksi benar).
  2) **Diskon**: `PaymentDialog` + section Diskon (ChoiceChip "Persen (%)"/"Nominal (Rp)" + field nilai, auto-format titik ribuan) → hitung `discountAmount` (clamp 0..total), tampilkan "Total Setelah Diskon", validasi cash `paid < grandTotal`, return `discount`. `PosProvider.completeTransaction(+int discount)` → `grandTotal = cartTotal - discount`, save `total_amount=grandTotal`, `change = paid - grandTotal`. Struk 3 service tampilkan `Subtotal`/`Diskon: -RpX` bila discount>0. `fetchTransactions`/`saveTransaction`/`fetchAllTransactions` + kolom `discount`.
  3) **Filter cabang**: `fetchTransactions({branchId})` & `fetchAllTransactions({branchId})` + filter `.eq('branch_id', ...)`; PosProvider.loadProducts pakai `getTransactions(branchId: currentUser.branchId)`; menu_screen `fetchAllTransactions(branchId)` → cabang lain TIDAK melihat transaksi cabang lain. (backup_service tetap `fetchTransactions()` tanpa filter = semua cabang, utk backup.)
  4) **Hold Order multi-pelanggan**: `HeldOrder` + `customerNames` (List) + getter join; `PosState.pendingCustomerName` → `pendingCustomerNames` (List<String>); `PaymentDialog.initialCustomerNames` (List) prefill; dialog Hold Order dinamis (StatefulBuilder, "+ Tambah Pelanggan", hapus per baris) → `holdCurrentOrder(customerNames: [...])`; `retrieveHeldOrder` restore list. `saveHeldOrder`/`fetchHeldOrders` + `customers` jsonb.
  - Build Windows sukses (67s); analyzer bersih (warning deprecation pre-existing).
- **2026-08-15 — Multi pelanggan & multi terapis per transaksi + terapis di struk**:
  1) **DB (via Supabase Management API)**: `transactions` + 2 kolom JSONB — `customers jsonb NOT NULL DEFAULT '[]'` (array nama) & `terapis jsonb NOT NULL DEFAULT '[]'` (array `{id,name}`). Backfill data lama dari `customer_name`/`terapis_name`/`terapis_id` (29 txn → 29 customers, 11 terapis). Kolom legacy tetap diisi (customer_name/terapis_id/terapis_name = join/pertama) utk kompatibilitas. Terverifikasi round-trip: insert 3 customers + 3 terapis → `["Budi","Siti","Andi"]` & `[{"id":"x1","name":"Dzikri"},...]` → OK, lalu dihapus.
  2) **App**: `Transaction` model → `customerNames`/`terapisIds`/`terapisNames` (List) + getter kompat `customerName`/`terapisId`/`terapisName` (join). `SupabaseService.fetchTransactions` parse JSONB (fallback legacy), `saveTransaction` tulis JSONB, tambah `getTerapisForDate(date,{branchId})` (distinct terapis utk struk harian/laporan, dari `terapis` JSONB + fallback `terapis_name`). `PaymentDialog` kini dinamis: tombol "+ Tambah Pelanggan" (list field, tiap field wajib, tombol hapus) & "+ Tambah Terapis" (chips multi, pilih dari picker per cabang). Repo/provider/screen mengalirkan list. Struk (3 service: thermal, Windows PDF, Windows BT) cetak SEMUA pelanggan & terapis; `generateDailySummary`/`generateClosingReport` + `printDailySummary`/`printClosingReport` tambah param `terapis` (section TERAPIS di struk). `menu_screen` ambil terapis utk ringkasan harian & laporan (tampil di UI + dikirim ke printer). Analyzer bersih (hanya warning deprecation pre-existing); build Windows sukses; launch test lulus (20 s, tanpa error log, ~143 MB).
  3) Catatan MCP/Management API: tool MCP Supabase TIDAK aktif di sesi VS Code ini → pakai **Management API** `POST https://api.supabase.com/v1/projects/<ref>/database/query` + `curl.exe` (Invoke-WebRequest gagal encoding). **Gotcha**: jangan `Get-Content -Raw` (hasil PSObject + PSPath) → pakai `[System.IO.File]::ReadAllText`; jangan `Set-Content -Encoding Utf8` (tulis BOM → 400) → `-Encoding Ascii`.
- **2026-08-15 — Panduan Pengguna (User Guide) dibuat**: `USER_GUIDE.md` (repo root) — panduan operasional berbahasa Indonesia untuk kasir/admin/karyawan: instalasi, login & akun (termasuk daftar akun staf + password default `dhbh12345`), tampilan utama (4 tab POS/History/Pelanggan/Menu), alur transaksi POS (shortcut `Ctrl+K` cari & `F9` bayar, Klinik/Home Visit, hold order), form pembayaran (pelanggan wajib, terapis, catatan, Cash/Transfer/QRIS), pencetakan struk (prioritas: BT thermal → printer Windows; tanpa printer transaksi tetap tersimpan), riwayat & refund, kelola pelanggan, ringkasan harian & laporan tutup kasir (modal awal), backup manual, troubleshooting (termasuk `%TEMP%\dhbh_flutter_errors.log`) & keamanan. Disusun dari PROJECT_CONTEXT.md, Database_details.md, AGENTS.md, Panduan.md.
- **2026-08-15 — Windows release build + optimisasi aset + installer Inno Setup (sesuai `Panduan.md`)**:
  1) **Build**: Flutter 3.47.0 / Dart 3.13.0, VS Build Tools 2022 + Win SDK 10.0.26100. `flutter clean && pub get && build windows --release` sukses → `build\windows\x64\runner\Release\dhbh_app.exe` (BINARY_NAME `dhbh_app` di `windows/CMakeLists.txt`). Release berisi exe, `flutter_windows.dll`, `data/app.so`, `icudtl.dat`, 9 plugin DLL (termasuk `flutter_thermal_printer_windows_plugin.dll`, `printing_plugin.dll`, `pdfium.dll`, `sqlite3.dll`).
  2) **Optimisasi logo (build-time, §10.1 Panduan)**: `lib/assets/logo-dhbh.png` 2048×2048/32bpp ARGB **4.05 MB → 256×256/96 KB** (bicubic high-quality, alpha preserved; dipakai ≤100×100 px). Backup asli di `build/logo-backup/logo-dhbh-2048.png`; skrip regenerasi `installer/optimize_logo.ps1`.
  3) **Icon aplikasi branded**: `windows/runner/resources/app_icon.ico` diganti dari icon Flutter default → ICO multi-size (16/24/32/48/64/128/256) dari logo (valid PNG-compressed entries; terverifikasi tertanam di exe via `Icon.ExtractAssociatedIcon` — 32×32 = 2510 bytes cocok). Skrip `installer/make_icon.ps1`. Backup icon default di `build/logo-backup/app_icon.ico.default`.
  4) **Installer**: `installer/dhbh_pos_installer.iss` (Inno Setup 7) — `ArchitecturesAllowed/InstallIn64BitMode = x64compatible` (hindari warning deprecated `x64`; dukung ARM64 emulation), per-user (`PrivilegesRequired=lowest`, tanpa UAC), desktop+Start Menu shortcut, launch post-install, `SetupIconFile` = icon branded. Output: **`installer/Output/DHBH-POS-Setup.exe` (13.9 MB, lzma2)**. Compile via `"C:\Program Files\Inno Setup 7\ISCC.exe"`.
  5) **Verifikasi end-to-end**: launch test berulang — exe jalan >20 s, TIDAK ada `%TEMP%\dhbh_flutter_errors.log`, RAM idle ~135–153 MB (aman utk PC 4GB). **Silent install test lulus**: `/VERYSILENT /DIR=%LOCALAPPDATA%\DHBH-POS-Test` → exit 0, semua file terpasang, app jalan 15 s tanpa error log, uninstall exit 0 & folder test bersih.
  6) **Dokumentasi**: `Panduan.md` (repo root) disimpan sebagai panduan build/packaging/install + bagian "Deployment Log" (§25) mencatat sesi ini. Catatan: terminal VS Code punya quirk (beberapa cmd inline gagal: `Copy-Item`, `tasklist`, `powershell`, `&`, `$var`) → jalankan via file `.ps1`/`.bat` + `powershell -File` dengan path absolut penuh. Login interaktif ke Supabase & print path tidak bisa diuji headless — perlu klien PC (ceklist §19).
- **2026-08-15 — Ringkasan harian & badge per cabang + sumber dari tabel `daily_summaries`**:
  1) **DB**: `daily_summaries` + kolom `total_transfer bigint NOT NULL DEFAULT 0`; fungsi `generate_daily_summary()` ditulis ulang — WIB date (`DATE(created_at AT TIME ZONE 'Asia/Jakarta')`), pakai `total_amount` (bukan amount_paid), mapping `cash`/`transfer`/`qris`, refund per cabang, **LEFT JOIN `branches` (is_active) sehingga SETIAP cabang mendapat baris** (zero-fill), dan **`SECURITY DEFINER SET search_path=public`** (RLS awalnya menolak INSERT dari `authenticated` → RPC app gagal `42501`; diubah agar app bisa regenerate). Tidak ada pg_cron → app memanggil RPC ini on-demand. Terverifikasi: 2026-08-15 → cabang 1 (Kranggan) 10 transaksi / Rp 3.515.000; cabang 2 (Cikampek) 0 transaksi / Rp 0.
  2) **App**: `fetchTransactions()` kini SELECT `branch_id` & set `Transaction.branchId`; badge hari ini (`PosProvider.todayTransactionCount`/`todayRevenue` + `MainAppScreen`) difilter per `currentUser.branchId`; `fetchDailySummary({branchId})` baca dari tabel `daily_summaries` (setelah RPC `generate_daily_summary` — karena semua cabang selalu punya baris, jarang fallback), **pastikan builder query di-reassign (`q = q.eq('branch_id', branchId)`) agar filter cabang benar-benar diterapkan**; fallback agregasi client-side per cabang; closing report (`getProductsSold`, `getPaymentBreakdown`, `getTransactionCounts`) + param `branchId`; Menu mengirim branchId; header "Ringkasan Harian" menampilkan nama cabang (subtitle). Terverifikasi di log: `fetchDailySummary TABLE: branch=2 tx=0 rev=0` (Cikampek).
  3) **Fix crash `UserRole.name` saat login**: `_buildAppBar(dynamic user)` → `_buildAppBar(AppUser? user)` (typing benar + import `models/user.dart`); `flutter clean` + rebuild menghilangkan NoSuchMethodError (stale incremental kernel).
  4) Catatan: startup kadang masih ada RenderFlex overflow transien (24px bottom) di login/banner — tercatat di `%TEMP%\dhbh_flutter_errors.log`.
- **2026-08-15 — Perbaikan lanjutan (MAC match, error log, menu desain)**:
  1) **MAC auto-connect match fix**: `matchesMac()` di `WindowsBluetoothPrinterService.autoConnect()` kini pakai `allMatches` (semua MAC) + `raw.contains(mac)` — device-id plugin berisi DUA MAC (adapter `04:7f:0e:7f:06:97` lalu printer `66:12:3f:23:ef:92`); `firstMatch` lama mengambil MAC adapter → "not found". Terverifikasi di runtime: `autoConnect → ...66:12:3f:23:ef:92...: true` (pair + connect sukses).
  2) **Error logging**: `main.dart` `FlutterError.onError` kini menulis exception lengkap + widget-tree infoCollector ke `%TEMP%\dhbh_flutter_errors.log` (diagnosis overflow/layout).
  3) **menu_screen.dart desain baru** (user): kartu "Produk Terjual" & breakdown pembayaran; fix compile `.take(15).asMap()` → `.toList().asMap()` (asMap hanya ada di List).
  4) **Plugin test mock** dilengkapi `isBluetoothEnabled`/`enableBluetooth`.
  - Held order dipastikan 100% DB-backed: `getHeldOrders` (fetch `active`), `saveHeldOrder` (insert + id asli), `completeHeldOrder` (status `completed`). Terverifikasi di log runtime.
- **2026-08-15 — Fix "Akun Tersimpan" login tidak muncul**: bug `LoginScreen._login()` memakai `if (!mounted) return;` setelah `login()` sukses — padahal `PosProvider.login()` sudah men-set `currentUser` lalu `await loadProducts()`, sehingga AppGate pindah ke MainAppScreen dan LoginScreen di-dispose → `_saveEmail()` tidak pernah jalan (terbukti `saved_login_emails` tidak ada di shared_preferences.json padahal auth-token ada). Diperbaiki: sukses path tidak lagi guard `mounted` → email tersimpan; bagian "Akun Tersimpan" dipindah ke atas form (sebelum field Email) agar langsung terlihat saat halaman login dibuka; tambah `debugPrint('[Login] saved account: ...')`.
- **2026-08-15 — Revisi desain & logic POS (batch 2)**: 
  1) `pos_screen.dart`: sidebar kiri dihapus; keranjang lebih lebar (Desktop 480px / Tablet 400px) dengan teks item lebih besar (`CartItemCard` font 14/13/15, tombol qty 28px); header desktop pakai `Flexible`+`Wrap` anti-overflow; popup "Pilih Jenis Layanan" diperlebar (max 520px di layar lebar); font di seluruh POS lebih tebal.
  2) Font global lebih tebal: `AppFormField` (teks w600, hint w400 + warna lebih gelap agar placeholder terlihat), `ProductCard`, tombol, footer cart.
  3) Auto-format uang dengan titik ribuan (helper baru `lib/utils/input_formatters.dart` — `ThousandsInputFormatter`): diterapkan ke "Jumlah Dibayar" (PaymentDialog) & "Modal Awal" (MenuScreen).
  4) Badge transaksi di AppBar (`main_app_screen.dart`) diperbaiki: warna abu-abu → `darkBlue`, font 11 → 14.
  5) **Fix held order hilang/tersisa**: `SupabaseService.saveHeldOrder()` kini `.select('id')` mengembalikan id DB asli; `HeldOrderRepository.saveHeldOrder()` memakai id asli (sebelumnya id lokal fiktif → `completeHeldOrder` tak menemukan baris → held order muncul lagi setelah restart).
  6) **Fix print tanpa printer**: tombol print di Menu (Ringkasan & Laporan) dan `printTransaction()` kini cek `btPrinter.isConnected || WindowsPrinterService.isPrinterReady`; jika tak ada printer → snackbar "Tidak ada printer terhubung" / return false (bukan sukses palsu).
  7) **AutoConnect printer saat login**: `WindowsBluetoothPrinterService.autoConnect()` (default MAC `66:12:3f:23:ef:92`) — cek & nyalakan Bluetooth Windows otomatis via native WinRT Radio (`isBluetoothEnabled`/`enableBluetooth` ditambah di plugin vendor `third_party/flutter_thermal_printer_windows`: `bluetooth_winrt.h/cpp`, `plugin.cpp`, Dart API) lalu cari di paired/scan, pair jika perlu, connect. Dipanggil di `PosProvider.login()`.
  8) **Login akun tersimpan**: daftar email yang pernah login disimpan di SharedPreferences (`saved_login_emails`) dan ditampilkan sebagai kartu "Akun Tersimpan" — klik mengisi email & fokus ke password; tombol × menghapus.
  - Analyzer bersih (hanya 2 info deprecation Radio pre-existing); build Windows sukses.
- **2026-08-15 — Redesign UI POS Screen (responsive 3 layout)**: `pos_screen.dart` dirombak total: breakpoint baru **Desktop ≥1200px** (sidebar 72px + header dengan search, shortcut hint `Ctrl+K Cari • F9 Bayar`, user profile, badge held orders; cart panel 400px), **Tablet 900–1199px** (2 panel tanpa sidebar, cart 360px), **Phone <900px** (single column + footer cart). Fitur baru: auto-focus search saat desktop, shortcut `Ctrl+K` (Shortcuts/Actions + `_SearchIntent`), `F9 Bayar` (label di tombol), `_calculateGridColumns` (5 kolom ≥1600px, 4 ≥1366px, 3 ≥1200px), chip kategori `AnimatedContainer`, helper `_SidebarIcon` & `_IconButton` (MouseRegion cursor click), empty-state keranjang lebih baik. Import `flutter/services.dart` ditambah; `responsive_utils.dart` tidak lagi dipakai di file ini. Analyzer bersih; build Windows sukses.
- **2026-08-15 — Cetak Ringkasan Harian & Laporan Tutup Kasir dipisah**: Sebelumnya tombol "Print Ringkasan" dan "Print" (laporan) sama-sama memanggil `printClosingReport()` → isi struk identik. Kini ada metode cetak terpisah `printDailySummary()` di 3 service (`windows_bluetooth_printer_service`, `windows_printer_service`, `thermal_printer_service`) — struk RINGKASAN HARIAN kompak (cabang/kasir/tanggal, Total Transaksi, Total Revenue, breakdown Cash/Transfer/QRIS/Refund). `_printDailySummary()` di `menu_screen.dart` memakai data `_summary` (fetchDailySummary) & dispatch sendiri (Windows BT → Windows printer → Android thermal). Laporan Tutup Kasir tetap memakai `printClosingReport()` (detail produk, modal awal, saldo akhir). Analyzer bersih.
- **2026-08-15 — Fix timezone WIB (UTC+7): transaksi/ringkasan/laporan/badge salah jam**: Supabase menyimpan `created_at` sebagai UTC, tapi app mengirim boundary query `DateTime.toIso8601String()` naive (tanpa `Z`) → Postgres anggap UTC → semua query "hari ini" selisih 7 jam (ringkasan harian & laporan tutup kasir kosong, badge transaksi di AppBar tidak bertambah). Diperbaiki: (1) semua boundary query di `fetchDailySummary()`, `getProductsSold()`, `getPaymentBreakdown()`, `getTransactionCounts()` kini `.toUtc()` dulu (`startUtc`/`endUtc`); (2) timestamp UTC dari DB dikonversi ke WIB saat tampil/dibandingkan via helper baru `lib/utils/wib_time.dart` (`WibTime.toWib` = `dt.toUtc().add(7h)`) — dipakai di struk (3 service printer), `HistoryScreen`, `MenuScreen`, badge `PosProvider.todayTransactionCount`/`todayRevenue` & `MainAppScreen`. Insert `transactions` TIDAK mengirim `created_at` (pakai default `now()` UTC) jadi data baru sudah benar. Terverifikasi via Supabase: query Aug 15 WIB dengan boundary UTC → 3 transaksi / Rp 1.930.000 (sebelumnya naive → 0). Build Windows sukses; analyzer bersih.
- **2026-08-15 — Fix printer MPT-II (SPP): koneksi tidak bertahan + karakter rusak + ID/status**: 
  1) **Auto-reconnect** di `WindowsBluetoothPrinterService` (`_printWithReconnect`) — koneksi SPP sering di-abort printer (`0x80072745 WSAECONNABORTED`) setelah satu job; sekarang gagal → disconnect → jeda 500ms → reconnect → retry. Native `BluetoothSendImpl` juga menghapus socket mati agar status koneksi akurat.
  2) **Karakter rusak ("roCro")** di divider struk: `'─'` (U+2500) tidak didukung charset printer → diganti `'-'` ASCII di semua divider receipt/closing report.
  3) **Receipt menampilkan `ID: #<order_no>`** (sebelumnya UUID lokal): `SupabaseService.saveTransaction()` kini mengembalikan `order_no`; `TransactionRepository.saveTransaction()` mengembalikan `Transaction` dengan `id=orderNo.toString()` & `orderNo` (bentuk sama dgn fetchTransactions). Struk Windows BT memakai `ID: #${t.orderNo ?? t.id}`.
  4) **Print status di History kini benar** (printed/failed): `PosProvider._tryAutoPrint` didelegasikan ke `printTransaction()` yg mengupdate `transactions.print_status` di DB + state lokal (sebelumnya auto-print lewat service langsung, status tetap unprinted).
  5) **Ringkasan harian Menu auto-refresh** saat jumlah transaksi berubah (via `ref.listen` di `MenuScreen`).
  - Catatan: MPT-II ternyata SPP (Serial Port "HS_SPP" COM20), BUKAN BLE. Semua kode BLE (enumerasi GATT, connect, model `isBle`) sudah di-REVERT dari plugin; plugin di-vendor tetap di `third_party/flutter_thermal_printer_windows` (path dep) dengan tambahan `/EHa` (anti-crash access violation) + is_paired safe.
- **2026-08-14 — Windows Bluetooth thermal printer via `flutter_thermal_printer_windows ^0.0.1`**: service baru `WindowsBluetoothPrinterService` (scan→pair→connect→ESC/POS `Receipt` 58mm auto-cut; singleton; `isSupported` = Windows). Provider `windows_bluetooth_printer_provider.dart` (status + paired devices streams). UI: `BluetoothStatusWidget` di Windows kini menampilkan status Bluetooth thermal; `BluetoothConnectionDialog` di Windows punya section Bluetooth (status, scan, paired list connect/disconnect, test print). Alur cetak (auto-print, print ulang History, closing report Menu) kini di Windows preferensi: **Bluetooth thermal → Windows printer (XP-58)** → ThermalPrinterService (Android). Catatan: plugin tidak mereset style antar item ESC/POS → tiap item Receipt diberi style eksplisit. Build Windows sukses; analyzer bersih (2 warning pre-existing Radio deprecation).
- **2026-08-14 — Pembayaran: Terapis + Note + metode Cash/Transfer/QRIS + Hold Order pilih pelanggan**: `PaymentMethod` disederhanakan jadi `{cash, transfer, qris}` (display: Cash/Transfer/QRIS); DB `transactions_payment_method_check` diperbarui + migrasi data lama (`debit`/`credit`→`transfer`, `e_wallet`→`qris`, hasil: qris 15, transfer 3). Kolom baru `transactions.terapis_id` (FK→user_profiles.id) & `terapis_name`; `transactions.notes` dipakai utk Note "Tips: Rp 50.000". Form bayar kini punya Terapis picker (filter role_id=3 + branch kasir login via `SupabaseService.fetchTherapists`) + textarea Note. Dialog Hold Order kini bisa pilih pelanggan (CustomerScreen picker). Receipt (Windows & thermal) & History/Menu menampilkan Terapis/Note; summary Menu jadi Cash/Transfer/QRIS. Dokumen PROJECT_CONTEXT.md & Database_details.md diperbarui.
- **2026-08-14 — Data cabang & karyawan diperbarui**: `branches` kini hanya 2 (DHBH Kranggan & DHBH Cikampek, cabang 3 dihapus); 20 karyawan dibuat sebagai auth.users + `user_profiles` + `profiles` (email `nama@dhbh.com`, password default `dhbh12345`). Kepala Cabang→admin, Admin/Kasir→kasir, Terapis→karyawan. Catatan: `profiles` punya CHECK constraint `role IN ('admin','superadmin')` → semua `profiles.role='admin'`; role POS aktual di `user_profiles.role_id`.
- **2026-08-14 — Database_details.md dibuat**: skema database Supabase diekstrak live via Management API (20 tabel + 1 view + 7 fungsi + 62 RLS policy). Temuan penting: TIDAK ada Postgres ENUM (semua text); tabel `posts`/`products_website`/`profiles` & view `products_sold_per_day` tidak tercatat di PROJECT_CONTEXT.md; `profiles.role` (admin/superadmin) dipakai `is_admin()`; trigger otomatis mengisi `activity_logs`.
- **2026-08-14 — MCP Supabase diperbaiki**: `com.supabase/mcp` di `%APPDATA%\Code\User\mcp.json` kini punya header `Authorization: Bearer sbp_...` (PAT). Reload VS Code window agar tools MCP aktif.

## Session Memory

- Supabase Management API (bukan MCP tools) dipakai untuk ekstraksi: `GET/POST https://api.supabase.com/v1/projects/<ref>/database/query` dengan `Authorization: Bearer <PAT>` (anon key TIDAK bisa). Endpoint OpenAPI `/rest/v1/` butuh service_role.
- Project Supabase: `Mako-Replicate` (ref `jiunlvlcwsntjbyybszd`, region ap-southeast-2, org `llqbsdoyxuzaufvvupox`).
- Anon key ada di `lib/config/supabase_config.dart` (aman untuk client-side).
